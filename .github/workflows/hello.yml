name: Hello Pipeline

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches:  [ "master" ]
  workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Exibir mensagem de início
        run: echo "Pipeline iniciado com sucesso"
